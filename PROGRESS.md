# MentorOS — Build Progress Log

> Your working memory across sessions. Read this first on any fresh session, then read `plan.md`.
> Update this at every phase gate and before ~60% context usage. **Never** move volatile state back into `plan.md`.

## Current status
- **USER DIRECTIVE (2026-07-06, overrides check-in gates):** auto-continue WITHOUT stopping for approval — finish Phase 5 (all 4 interview types, still built one at a time), then Phase 6, and only stop after Phase 6 is done or the session limit is hit. Keep hard checkpoints (commit + PROGRESS.md) at every slice so a limit cutoff loses nothing. Still run verifier + design-reviewer per slice.
- **Phase:** 5 — **slice 1 (Coding interview) COMPLETE** (built, runtime-verified, design-reviewed, fixes landed, 75/75 tests green, tsc×2 + electron-vite build green). Spec followed = plan.md §4.5 + interview-prep CLAUDE.md protocol.
- **Coding slice shipped:** contract in coreClient (`/interview/*`, events interview.token/status/phase/scorecard); core `src/core/interview/*` — 10-problem bank (LC 1/3/20/33/42/53/56/121/200/322, own-words prompts, py+js starters, 10-12 hidden tests + 3 hints each), out-of-process eval runner (python3/node, 10s deadline, compileError/timeout paths), phase-scoped zero-praise interviewer (llama3.1), LLM-JSON scorecard w/ deterministic offline fallback + §5 spaced-rep dates + memory writeback (skill/mistake/learning via upsert), weakness→pattern recommendation; renderer — launcher (4 cards, Coding live), ProblemPicker (keyboard-first), session screen (problem md | transcript w/ mini orb | Monaco `monaco-editor@0.55.1` bundled offline via vite ?worker | results drawer), Scorecard overlay w/ "Profile updated" memory chips; interviewStore.
- **Runtime-verified (all pass):** framing opener streams (fixed llama3.1 empty-completion-without-trailing-user-turn via synthetic nudge); LLM-judged framing→coding via stripped [BEGIN CODING] marker (verified: transitions on good framing 2nd exchange, pushes back on bad, marker never leaks; /run//hint stay fail-open transition); hint ladder 1-3 then 409; eval correct/incorrect/syntax/timeout; interrogation opener = the 5 §3.1 questions; scorecard ~10s w/ correct +4d/+10d interval math + 3 memories written + memory.saved fired; resume restores session.code; js runner path; Ollama-down degrade + offline fallback scorecard + recovery; no log errors.
- **Design-review fixes landed:** bodyless POSTs no longer send JSON content-type (fastify 400 broke End-interview/Hint/Abandon from real UI — CRITICAL, fixed in coreClient post(); json() now tolerates 204/empty); WS-beats-HTTP race guarded via terminalTurns set + functional set() merges in send/hint/finish (was stranding turnPhase='thinking', hint disabled forever when Ollama down); ProblemPicker keyboard-first (autofocus list on open, onFocus syncs selected — Tab+Enter used to start the WRONG problem, arrows now work immediately, scrollIntoView); recommended-row focus ring visible (ring→outline, box-shadow collision); transcript autoscrolls on turnError.
- **Deferred drift (fold into a later slice/Phase 7):** (a) ContextPanel doesn't auto-collapse <~900px → session toolbar overflows under it (known Phase-1 nit, now functional on this screen — fix when touching shell); (b) scorecard light-theme section labels (--muted on white) read faint; (c) no persistent back-to-launcher affordance on session screen outside error path; (d) renderer main chunk 10.4MB (monaco in index bundle — lazy-load interview screen in Phase 7 perf pass).
- **Screenshots:** scratchpad `/p5a/` (launcher/picker/framing/results/hints/interrogation/scorecard/degraded, dark+light).
- **RE-PLAN (user, 2026-07-06): Phase-1 feedback backlog is pulled FORWARD; Phase 5 slices 2-4 (SQL, System Design, Behavioral) and Phase 6 are pushed to LAST.** New order: (A) Voice-upgrade slice = TTS voice picker (Kokoro multi-voice) + STT quality option (medium.en / large-v3-turbo download) + mentor face/avatar on the Voice screen + minimal Settings screen to host the options; (B) remaining feedback items (interim live transcripts, model switching incl. cloud, VAD/wake-word) as follow-up slices as the user confirms; (LAST) Phase 5 slices 2-4, then Phase 6.
- **Interview UX bugfixes landed (commit fd0e004):** interrogation composer was disabled (candidate couldn't answer the 5 follow-up questions — locked now only in scorecard/abandoned; core say() already handled interrogation turns); after scorecard dismiss there was no exit — toolbar now shows "View scorecard" (reopen overlay) + "Back to interviews" (launcher) in terminal phases. tsc×2 + 75/75 tests green; verifier runtime-verified BOTH (interrogation answer→streamed follow-up; View scorecard/Back to interviews round-trip to launcher; zero console errors; screenshots scratchpad `/p5fix/`).
- **Voice-upgrade slice COMPLETE + VERIFIED (380e53e + mouth-sensitivity fix).** Verifier runtime pass 7/7: settings nav (rail gear + ⌘K), core API (28 en voices, stt-models states, settings roundtrip), picker UI (select persists, preview WAV 200/audio-wav + play/stop states, unknown voice 400), voice change e2e (bf_emma speak OK — note: /voice/speak 409s until Voice screen opens the WS channel, by design), mentor face dark+light incl. envelope-driven mouth + toggle-back-to-orb, ZERO console errors, chat/interview smoke OK. Screenshots scratchpad `/pvoice/`. Design audit done by Fable directly on captured screenshots (images already in context — cheaper than re-driving app via design-reviewer): §3.0 invariants hold (surface ladder, monochrome chrome + color only on orb/state, keyboard-first, dark+light); one fix landed = sqrt perceptual curve on TTS envelope so the speaking mouth opens visibly at real speech RMS. USER CONFIRMED NEXT (2026-07-06 PM): (1) **Model switching slice** — app-wide model selection, local (Ollama) + cloud (Claude API et al) per §2.4 router; API-key settings; per-surface model use (chat/voice/interviewer/scorecard); THE POINT: user's interview-prep review flow currently runs on Opus in Claude Code and must not lose quality — once cloud models work in-app, that flow moves in-app. Look up current model IDs/pricing when building (claude-api skill). (2) **Face gallery slice** — several realistic-styled female face presets (stylized-realistic, lip-synced like MentorFace; true photoreal talking-head ML is not offline-feasible) selectable in Settings. (3) **In-app problem importer** for interview bank (paste LC problem → generate starters/tests/hints w/ review; much better once cloud models land). Then interim transcripts/VAD/wake-word, then Phase 5 slices 2-4 + Phase 6 LAST.
- **Transcription-timeout bug FIXED (0ea6510):** cause = fixed 8s renderer deadline vs whisper cold start (~7s one-time Metal shader compile) → timeout fired, then late final transcript displayed but never answered (orb already idle). Fix: core warmStt() on /voice WS open (200ms silence, re-warms on model change) + deadline 20s + late-final recovery (clears timeout error, still answers). 92/92 green.
- **Model-switching slice COMPLETE + VERIFIED (3961e83 + design fixes 545db80).** Verifier 8/8 (providers shape, key lifecycle w/ live-validated bogus key → invalid/masked, Models UI dark+light, fallback routing fellBack:true + local stream w/ "Running locally" footer, per-surface status routes, voice surface:'voice' functional, regression smoke chat/interview/voice, zero console errors) + focused re-verify 3/3 after design fixes (no label truncation DOM-measured, opaque surface-2 popover both themes, click-outside/Escape/selection). Screenshots → ~/Desktop/mentoros-screenshots/pmodels/. Design fixes: cost hint moved to note line, popover opaque (glass reserved per §3.3), rows surface-3, click-outside via document listener (fixed backdrops are neutered by ancestor motion transforms — reusable lesson). NEEDS USER LIVE: paste real Anthropic key in Settings→Models, verify cloud streams on chat/interviewer/scorecard (billing is user's). Slice detail: Surfaces = chat/voice/interviewer/scorecard (voice rides POST /chat `surface:'voice'`; memory merge-judge stays local-only by design). Contract in coreClient.ts: ModelProvider/ModelChoice/ModelSurface, AppSettings.{cloudEnabled,models}, ProvidersInfo (key masked-only), listProviders/setAnthropicKey/clearAnthropicKey, modelStatus(surface?), sendMessage(...,surface?). Core `src/core/llm/*`: anthropic.ts (@anthropic-ai/sdk; CLOUD_CATALOG = claude-opus-4-8 $5/$25 recommended, claude-sonnet-5 $3/$15, claude-haiku-4-5 $1/$5, claude-fable-5 $10/$50 — ids current 2026-06, NO date suffixes; system-role msgs hoisted to top-level system, omitted when empty; adaptive thinking except haiku; errors humanized at adapter boundary via typed SDK classes, aborts pass through; refusal stop → designed error; key validation = models.list w/ 6s timeout), keys.ts (write-only KeyStore, `keys.*` rows in settings table, masked reads, SettingsStore rejects keys.* patches), router.ts (resolve→silent fallback to ollama llama3.1:8b when cloud unusable; stream/once/status dispatch), routes.ts (GET /models/providers, PUT/DELETE /models/keys/anthropic, GET /models/status?surface=). ChatEngine/Interviewer/scorecard wired through router (scorecard via injected `once`, offline fallback intact). Renderer: ModelsSection.tsx (cloud toggle, key row w/ validate/mask/remove, 4 surface pickers w/ local+cloud groups), Switch.tsx primitive, settingsStore actions, voiceStore sends surface:'voice'. Gate: tsc node+web green, 109/109 tests, electron-vite build green. Core-engineer agent died at session limit mid-task; Fable finished: stale settings round-trip test, models.list timeout→request-options arg, 2 stale AppSettings literals in voice/manager.ts + voice.test.ts, error humanization + empty-system omission in adapter.
- **FACE GALLERY slice — v1 BUILT + RUNTIME-VERIFIED, then USER PIVOT (wrap cutoff 2026-07-07).** Shipped in this commit: contract `mentorFace: FacePresetId ('aura'|'nova'|'ivy'|'zara'|'elle'|'mira'|'rae')` + `faceGlam ('natural'|'polished'|'glam')` + `faceMaturity ('youthful'|'balanced'|'mature')` in coreClient.ts + core types.ts + settings store validation + tests (110/110 green, tsc×2 green); renderer `orb/faces/presets.ts` (6 women as pure data: palette/geometry/hair paths) + `orb/faces/FacePortrait.tsx` (one parametric SVG portrait renderer — rAF blink/gaze/brow/head-sway/breathing, TTS-envelope articulated lips w/ teeth, state-hue aura+rim, glam/maturity live morphs, unique ids via useId, frozen mode); MentorAvatar routes 'face'+portrait→FacePortrait cameo (aura keeps orb+MentorFace overlay); IdentitySection full gallery (7 cards w/ hover-animate, 200px live preview, Styling/Presence segmented rows, setMentorLook store action). CDP-verified live: gallery renders, selection/styling rows work, Nova preview animates. Screenshot recipe: dev w/ `--remote-debugging-port=9222`, driver at scratchpad `snap.mjs` (volatile — rewrite from decisions-log recipe if gone).
- **USER FEEDBACK on v1 (drives the rework — read before resuming):** current portraits read "cartoonish/chibi"; user wants (1) keep only 2-3 of the stylized SVG faces; (2) a NEW separate category of much more realistic, attractive presets per reference images saved at repo `img/` (normal.jpg → mediam.webp → hot2.jpeg/hot.jpg → hot3.jpeg = escalating attractiveness levels the user wants selectable); (3) two view modes: face-size (cameo) AND full-body. CONSTRAINTS for the rework: (a) the reference images are photoreal AI pin-ups (lingerie/bikini) — keep in-app art **tasteful/non-explicit** (attractive full-body presets in normal attire are fine; no lingerie/softcore escalation — tell the user this boundary plainly at check-in); (b) hand-authored SVG cannot reach photorealism — the honest options to present: (i) much higher-craft vector portraits (better shading/proportions, still stylized), (ii) a local image-gen sidecar (SDXL-class via e.g. ComfyUI/sd-server, offline-capable, generates preset stills once + we lip-sync a mouth layer on top), (iii) bundled pre-made art assets. Recommend (ii) as the only path to "not cartoonish" — needs user decision + disk/VRAM budget before building.
- **Face-slice remaining work (v1 polish, still worth landing for the kept 2-3 presets):** art fixes queued from 3× zoom audit — Nova hairFront right-temple spike (smooth inner edge), nose bridge strokes too long/prominent (shorten y106-116, opacity ~0.1, nostril ellipses→opacity 0.35 smaller), neck shadow bands whole neck (clip to neck path, cy~143 opacity 0.4), brows slightly thicker (+0.3); mouth/iris/lash round-1 fixes ARE applied (mouth smaller+lighter: cornerY smile*1.15, lipLower*1.45; irisR 4.35; lash inner taper) but NOT visually re-verified — re-zoom Nova + all presets, then speaking/blink states, light theme, Voice-screen integration (MentorAvatar swap at 340px), verifier pass, design audit, commit.
- **FACE REWORK DECIDED (user, 2026-07-07):** (1) realistic category = **local image-gen sidecar** (SDXL/FLUX-class on M4 Pro 24GB; ~7-10GB disk OK, 38GB free) — generate preset stills ONCE (portrait cameo + full-body), lip-sync via animated mouth/eyelid layer on top; (2) stylized keep set = **nova + rae + ivy** (drop zara/elle/mira; aura orb-face stays); (3) tasteful boundary stated to user and accepted: normal attire, no lingerie/softcore escalation — attractiveness axis = styling/glam/pose only.
- **Next step (exact):** (1) researcher: best local image-gen for Apple Silicon 2026 (mflux/FLUX-schnell vs stable-diffusion.cpp vs alternatives; inpainting support for mouth-state variants; model download source+size; CLI usage) → pick toolchain. (2) Fable: contract deltas — trim FacePresetId to aura|nova|ivy|rae + add realistic ids, preset `kind:'stylized'|'realistic'`, `faceView:'cameo'|'full'` in AppSettings, settings migration for removed stored ids → fallback 'aura', tests. (3) Land queued v1 art fixes on kept presets (Nova hairFront right-temple spike; nose bridge strokes shorter/opacity ~0.1, nostril ellipses opacity 0.35 smaller; neck shadow clip to neck path cy~143 opacity 0.4; brows +0.3) then re-zoom verify. (4) Agent installs toolchain + downloads model (background); Fable writes generation prompts (3 realistic presets × portrait + full-body + mouth/eyelid variant crops for sprite lip-sync). (5) RealisticPortrait renderer (base still + envelope-driven mouth sprite swap + blink overlay + breathing/sway transforms + Nocturne aura/rim), Settings gallery w/ category groups + view toggle, MentorAvatar/VoiceScreen wiring for full-body. (6) verifier + design audit + commit. Files: orb/faces/presets.ts, FacePortrait.tsx, new orb/faces/RealisticPortrait.tsx, MentorAvatar.tsx, screens/settings/IdentitySection.tsx, lib/coreClient.ts + core types.ts/settings store+tests; generated assets under apps/desktop/resources/faces/ (or similar). THEN: in-app problem importer, then interim transcripts/VAD/wake-word, then Phase 5 slices 2-4 + Phase 6 LAST.
- **Screenshots**: session scratchpads are volatile (/private/tmp, cleared on reboot/session) — current sets copied to `~/Desktop/mentoros-screenshots/` (pvoice, p5fix). Slice files: core src/core/settings/* + voice/voices.ts + voice/sttModels.ts + tts/manager/routes/stt/paths edits; renderer lib/settingsStore.ts + screens/settings/* + orb/MentorFace.tsx + orb/MentorAvatar.tsx + VoiceScreen (MentorAvatar swap) + Rail/App/CommandPalette/store nav wiring.
- **Voice-upgrade slice plan:** (1) Fable: contract deltas in coreClient.ts — AppSettings {ttsVoice, sttModel}, GET/PATCH /settings, GET /voice/voices (enumerate from voices-v1.0.bin, en voices af_/am_/bf_/bm_), GET /voice/preview?voice= (WAV one-shot for picker), STT model registry GET /voice/stt-models + POST download w/ voice.model.progress events; (2) core-engineer: settings KV table + routes, voice plumb into tts.ts synthesize (script already takes --voice), whisper model downloader (HF ggerganov/whisper.cpp: ggml-medium.en.bin, ggml-large-v3-turbo.bin) + stt uses settings model w/ fallback small.en; (3) builder: Settings screen (rail Settings button currently dead) — Voice section: voice cards w/ preview play, STT quality selector w/ download progress, face/orb toggle; (4) Fable: mentor face on Voice screen (avatar layered on orb aesthetic, TTS-envelope mouth, blink/idle, monochrome+aurora per §3.0); (5) verifier + design-reviewer + commit.
- **(Superseded by re-plan, resume LAST): Phase 5 slice 2 = SQL interview** (plan.md §4.5: schema panel + query editor + execution-plan/result view; scorecard→Memory same as coding). Plan: (1) Fable defines contract deltas in coreClient.ts (likely: extend InterviewType usage, SqlProblem w/ schema DDL + seed rows + expected result set, runSql via better-sqlite3 in-memory — NO new deps; EXPLAIN QUERY PLAN for the plan view; reuse session/turn/scorecard machinery — aim for InterviewProblem.sql? extension + one new run route or reuse /run w/ language 'sql'); (2) core-engineer: sql problem bank (~6-8 problems on a realistic schema e.g. a jobs/applications or orders dataset, hidden expected results, hints) + executor (in-memory sqlite, row-set compare order-insensitive unless ORDER BY required, EXPLAIN capture) + interviewer prompts (SQL-specific interrogation: indexes, plans, normalization trade-offs); (3) builder: SQL session screen (schema sidebar + Monaco sql language contribution + results table + plan view tab), launcher card enable; (4) verifier + design-reviewer + commit. THEN slice 3 = System Design (whiteboard — react-force-graph NOT suitable; draggable nodes/arrows custom SVG per §4.5), slice 4 = Behavioral (voice, STAR hints), then Phase 6 (Codebase Mentor §4.8 + Career Coach §4.9). Per user directive: no approval stops until Phase 6 done.
- **Design-review fixes (all committed):** markdown reading renderer (`ReadingMarkdown.tsx` — headings/GFM tables/lists/blockquotes; relative links switch files in folder sources; external links → system browser via new main-process setWindowOpenHandler); container-query KB grid + header reflow (900px OK); citation reconciliation in chat.ts (pills list only sources the answer actually cites [n], full-list fallback if model cites none — verified live, filtered 5→1); Add-source popover anchored + real --focus rings; **honest search** — FTS strict-AND w/ OR fallback only ≤2-term queries + vector floor 0.6 (measured nomic cosines: junk tops ~0.57, real semantic 0.72+; "zzzz"/"pizza toppings" → 0 hits → designed empty state (screenshot verified), keyword+NL queries still strong). 50 tests green.
- **Built (this commit):** KB contract in `coreClient.ts` (+ `ChatMessage.citations`); preload `getPathForFile` bridge + `renderer/lib/nativeBridge.ts` (Electron 43 removed File.path); core `src/core/kb/*` — store (kb_sources/kb_chunks/FTS5 `kb_chunks_fts`/kb_chunk_vectors, sanitized FTS queries), chunker (~800/150, md headings→section, pdf pages→`p. N`), ingest (md/txt/pdf via pdf-parse, folder walk, idempotent chunk ids, FTS-only degrade when Ollama down, home-dir path guard), search (RRF k=60, matched flags, snippets), suggestions (DSA patterns + SD concepts), routes incl. POST /kb/sources/:id/open; chat.ts grounding (gate: top hit fts/both, or vector cosine ≥0.45; max 5 excerpts ≤700ch; emits chat.sources; persists citations_json); renderer KB screen `screens/knowledge/*` + `lib/kbStore.ts` (grid/suggestions/drag-drop+progress toasts/search view/reading view/all states); chat citation pills ([n] superscripts, source-pill row) + live ContextPanel "Sources cited". 50 tests green, both tsc green, electron-vite build green.
- **Phase 4 verified (runtime, 10/11):** suggestions offered; patterns folder 9 files/83 chunks + real PDF 24 chunks ingested w/ kb.ingest events; hybrid search 8 hits w/ matched flags; grounded chat → chat.sources 5 citations, [n] in answer, persisted across restart; Ollama-down → FTS-only search AND ingest still work, recovers to vector after `open -a Ollama`; re-ingest idempotent (same sourceId, 83→83); reveal route 204/404. Citation pills deep-link into the source's reading view. FIXED: LibraryGrid zustand selector returned fresh array → infinite render loop → blank screen (filter moved outside selector).
- **Wi-Fi-off check = needs the user live** (an agent cutting Wi-Fi kills its own API link; all traffic is localhost so Ollama-down is the real degradation test — passed).
- **Next step (exact):** (1) collect **design-reviewer** result (KB library/search/reading + grounded chat, dark+light, screenshots → scratchpad `/p4/`); fix any drift it reports (files: screens/knowledge/*, chat/AssistantMessage.tsx, shell/ContextPanel.tsx). (2) Commit fixes. (3) **Hard check-in**: Phase 4 delivered summary w/ screenshots; offer Phase 5 = Coding interview first (one type per window per plan.md).
- **Phase 2+3 verified:** upsert-by-similarity = cosine gate + lexical guard + **llama3.1 merge-judge** (paraphrases merge, distinct facts keep, judge fails open to create; ~2s judged saves); interview-prep import 30 records idempotent (0/30 on re-run); recall injected into chat/voice with chat.context trust events; 3mc import 147 days/785 tasks idempotent with week focus topics; mission = 2-3 plan tasks + due review + weakness drill (top-up if plan imported mid-day); streak/XP/heat persist across restart (verified); linkPass gives graph edges; 42 unit tests green.
- **Known gaps (deliberate):** graph edges sparse (2 on current data — linking is title-in-body only); voice auto-capture keywords are heuristic; review-queue records that collapsed pre-judge in the old DB stay merged; day rows show generic titles (topic lives on the week label).
- **Screenshots:** scratchpad `/2a/` (memory profile/graph/drawer) and `/p23/` (home mission + fill, learning path, graph with edges).

## User's real-data sources (reviewed 2026-07-05 — import these, they replace synthetic seed)
- **`~/Documents/abhay/3-month-challenge/`** — 147 `day-*.md` (21 weeks: DSA + infra skill per day) with a WORKING parser → `study-ui/data/parsed-plan.json` (stable task IDs `phase-n-week-w-day-d-lc-num`, typed tasks, difficulty). No persisted progress (only localStorage in their study-ui) → MentorOS Learning Engine becomes the real tracker. User says daily volume is too heavy — mission selection should right-size.
- **`~/Documents/abhay/interview-prep/`** — 86 md, ~1 week of data, but PRESCRIPTIVE schemas: `recurring-mistakes.md` (mistake tally: Complexity miscalc ×8, Missed optimization ×4…), `review-queue.md` (spaced-rep grades 0-5 + ISO next-review dates), `patterns-learned.md` (confidence 1-5), per-problem interrogation scorecards, journal. Maps ~1:1 to memory types (mistake/skill/learning). `DSA/patterns/` (9 playbooks) + `System Design/concepts/` → Phase 4 KB import. Its CLAUDE.md interview protocol (Frame→Interrogate→Scorecard) is the spec to reuse for Phase 5.

## Improvement backlog (user feedback at Phase-1 gate — schedule in Phase 7 unless pulled earlier)
- STT accuracy "a bit sloppy" → offer whisper `medium.en` / `large-v3-turbo` as a quality option (small.en stays default for latency).
- Only one TTS voice → voice picker (Kokoro ships ~54 voices; cheap win, Settings).
- Avatar/"face" for the mentor (big; design exploration later — Orb stays the primary identity).
- **Model switching incl. cloud** (Claude Opus / Fable via the §2.4 router, opt-in + budget) — Phase 7 cloud adapters; look up current model IDs/pricing then.
- Interim (live) transcripts while speaking; hands-free VAD barge-in; wake word (onboarding phase).
- **1b verified:** streaming ladder over WS; stop→partial persistence; Ollama-offline designed banner + error event; SQLite at userData/data. Screenshots `/1b/`.
- **1c verified:** whisper small.en arm64+Metal (~514ms warm) + Kokoro af_heart (~560ms TTFC) sidecars, /voice WS matches contract exactly; UI: real TTS → orb speaking → idle on tts-end; interrupt ducks; 4 orb states visually distinct; reduced-motion FallbackOrb OK. Screenshots `/1c/`.
- **Needs the user live (first run):** macOS mic permission prompt → real hold-Space voice loop; network-off repeat; ⌥Space may need Accessibility permission; first whisper call pays one-time ~7s Metal shader compile.
- **Known 1c gaps (deliberate):** no interim transcripts (final on release only); barge-in is PTT/tap-triggered, hands-free VAD later; wake word deferred to onboarding phase.
- **Approved Phase-1 execution plan:** `~/.claude/plans/read-plan-md-and-eager-blossom.md`.

## Decisions log
- Node via nvm v22.16.0 (`.nvmrc`), pnpm via corepack. Machine default node is v12 — always `export PATH="$HOME/.nvm/versions/node/v22.16.0/bin:$PATH"`.
- Stack pins: electron 43, electron-vite 5, vite 7 (peer range), react 18.3, tailwind v4 (`@theme inline` maps Nocturne vars), fastify 5 + @fastify/websocket, motion, zustand, lucide, fontsource variable fonts (bundled).
- Core server runs in-process in Electron main (core has zero electron imports); port 4820 scan-up; renderer discovers via `?corePort=` query param.
- Added @fastify/cors (localhost origins + `null` for packaged file://) — renderer health fetch was CORS-blocked in dev (verifier caught it).
- Default local model: `llama3.1:8b` (installed; clean streaming — qwen3 emits <think> blocks).
- Design-review nits deferred: context panel should auto-collapse under ~900px width; light-theme `--faint` contrast re-check when real screens land.
- CDP screenshot recipe: `pnpm --filter @mentoros/desktop dev -- --remote-debugging-port=9222` (single `--`), drive via ws + Page.captureScreenshot. 1a screenshots in scratchpad `/1a/`.

- 1c research (done): STT = whisper.cpp (`brew install whisper-cpp` or source w/ GGML_METAL), model `ggml-small.en.bin` (466 MiB, HF ggerganov/whisper.cpp); no native WS streaming — use whisper-stream/server or wrap. TTS = **Kokoro instead of Piper** (Piper repo archived Oct 2025, python-only fork, seconds-level latency; Kokoro ~100ms TTFA on M4 Pro via kokoro/kokoro-mlx, 24kHz PCM streaming). Piper fallback: OHF-Voice/piper1-gpl + en_US-lessac-medium.
- Voice-loop state machine + CSS FallbackOrb already written (`renderer/orb/orbState.ts`, `FallbackOrb.tsx`) — shader Orb + audio plumbing still pending in 1c.

- better-sqlite3 must be built for **arm64** (nvm node is x86_64/Rosetta but Electron is arm64): postinstall pinned to `electron-rebuild -f -w better-sqlite3 --arch arm64`.
- 1b core-engineer run was killed by a session limit mid-task; Fable finished the last wiring (dataDir passthrough, arm64 rebuild) per the escalate-once rule.
- `pkill -f ollama` is case-insensitive-trap: menubar app is `Ollama` (capital) and respawns `ollama serve`; use `pkill -9 -f -i ollama` in tests, restore with `open -a Ollama`.

## Open questions for the user
- (none)

## File map
- `plan.md` — the spec (read-only during builds) · `CLAUDE.md` — agent instructions · `PROGRESS.md` — this file
- `apps/desktop/src/main/` Electron main · `src/core/` Fastify core server · `src/preload/` empty seam
- `src/renderer/theme/` tokens.css + ThemeProvider · `motion/springs.ts` · `ui/` primitives (Button, Card, Chip, Keycap, Panel, Overlay, Toast, Spinner)
- `src/renderer/app/shell/` AppShell, Rail, TitleBar, ContextPanel, CommandPalette · `app/screens/` Showcase, Placeholder
- `src/renderer/lib/` coreClient.ts (typed boundary — Fable owns the contract), store.ts, seed.ts, cn.ts

## Phase checklist
- [ ] Phase 1 — First Win: Voice + Orb magic moment (Stages 1a foundation/design-system, 1b local chat, 1c voice+orb)
- [ ] Phase 2 — Knowledge Memory
- [ ] Phase 3 — Daily Loop (Home + Learning)
- [ ] Phase 4 — Knowledge Base + Hybrid Search
- [ ] Phase 5 — Interview Platform
- [ ] Phase 6 — Codebase Mentor + Career Dashboard
- [ ] Phase 7 — Polish, Ownership & Optional Cloud
